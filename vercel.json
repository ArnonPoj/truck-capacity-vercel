{
  "version": 2,
  "functions": {
    "api/wsgi.py": {
      // 🚨 เปลี่ยนมาใช้ "now-python" ซึ่ง Vercel รองรับในรูปแบบ Serverless functions
      "runtime": "python3.9" 
    }
  },
  "routes": [
    // 1. กำหนดให้ Request ที่ไปที่ /static/ ถูกเสิร์ฟจากโฟลเดอร์ static
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    // 2. กำหนดให้ Request ที่เหลือทั้งหมด (รวมถึง / และ /api/compute) ถูกส่งไปที่ Flask app (wsgi.py)
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ]
}
